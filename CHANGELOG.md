# 更新历史

## 未发布 (Unreleased)

### 新功能 ✨
- **Catalog 分页浏览**：4chan 目录原本只能看第一页，现加入分页器，支持「上一页 / 下一页 + 页码」翻阅后续页内容
- **原图下载**：图片查看器右下角新增 `⬇` 下载按钮；帖子缩略图悬停时右下角也显示下载入口。下载走 VSCode 原生保存对话框，**原图无损保存**（宿主 Node 环境抓取，规避 webview 白屏问题）

### 改进 🛠
- 分页器**响应式自适应**：页码数量按侧边栏实际宽度动态计算（实测 DOM 尺寸，非硬编码估算），宽则多显、窄则少显，宽度不足处用 `…` 省略号收束；上一页 / 下一页始终固定在两端，**恰好占满中间宽度且绝不溢出**
- Catalog 卡片**响应式铺满宽度**：改用 `auto-fit` 布局，列数随宽度增减、整行无留白；卡片图片恒为**正方形**（`aspect-ratio: 1`），尺寸随列宽等比缩放，最后一页内容少时也不再被竖向拉伸变形
- CSP 新增 `connect-src` 放行，支持 webview 内的原图请求

## 1.0.5 (2026-08-05)

### 新功能 ✨
- 动图预览：图片查看器升级为**图片/视频双模式**，自动用 `<video>` 渲染 webm/mp4 动图（播放 / 循环 / 音量控制）
- 进入 **NSFW 板块**前弹出**页内遮罩确认框**（含免责声明：内容均来自 4chan，版权归原发布者所有，与本扩展无关），确认后才加载

### 改进 🛠
- 原图加载**优先直连** `i.4cdn.org`（`no-referrer` 规避防盗链 403），被拦截时自动降级为扩展宿主抓取转 base64（不落盘）
- **默认勾选 SFW**；取消勾选时**停留在当前板块**，不自动切换
- 启动时**永不默认加载 NSFW 板块**：上次停在 NSFW 板块则回退到第一个 SFW 板块
- 切换板块时**清空上个板块内容**并显示"加载中…"，且忽略过期响应，避免串板块/内容残留

### Bug 修复 🐛
- 修复动图打开无反应：webm/mp4 无法用 `<img>` 渲染的问题
- 修复图片预览成功加载时顶部出现空红框（缺少 `.overlay-err` 隐藏规则）
- 修复切换板块后上个板块（尤其 NSFW）内容残留的问题

## 1.0.4 (2026-08-05)

### 新功能 ✨
- 高级设置：每个 AI 供应商新增**模型选择下拉**，可选官方型号（默认选中最便宜的型号）
- 高级设置：API Key 已配置时以**密文展示**，**保存按钮**仅在输入时出现、保存后自动隐藏

### 改进 🛠
- 「选择翻译引擎」菜单的选中态现在与**当前实际引擎一致**：当前引擎带勾选标记 + "当前使用"标识，并默认聚焦到当前引擎（不再误显首项为选中）
- 更新各 AI 供应商模型列表为 2026 当前型号，DeepSeek 默认从已下线的 `deepseek-chat` 改为 `deepseek-v4-flash`

## 1.0.3 (2026-08-05)

### Bug 修复 🐛
- 修复侧边栏很窄时板块下拉框文字与下拉图标重叠的问题

## 1.0.2 (2026-08-05)

### 新功能 ✨
- 图片预览支持**滚轮缩放**（以鼠标位置为缩放中心）
- 图片预览右上角新增 **✕ 关闭按钮**（也可 `Esc` 关闭）
- 进入图片预览时**锁定背景滚动**，避免误滚下方内容

### 改进 🛠
- 侧边栏**顶部工具栏固定**，仅下方内容区滚动
- 帖子页**返回栏固定显示**（滚动时始终可见，两端对齐布局）
- 修复顶部固定栏背景半透明、滚动时**内容透出**的问题
- **翻译设置按钮**移至工具栏最右侧
- 单帖翻译完成后**保持滚动位置**，不再跳到顶部
- 帖子正文文字**两端对齐**（justify）
- 字体随侧边栏宽度**自适应放大**
- 滚动条贴侧边栏最右侧，采用 VSCode 风格细滚动条

### 变更 ♻️
- 图片预览**移除点击空白关闭与拖拽平移**（仅保留滚轮缩放 + 关闭按钮 / `Esc`）

## 1.0.1 (2026-08-05)

### Bug 修复 🐛
- 修复 Marketplace / 扩展列表显示默认齿轮图标的问题（补充扩展展示图标）

## 1.0.0 (2026-08-05)

### 首版发布 🎉

- 侧边栏浏览 4chan 板块，Catalog 卡片式帖子展示
- 帖子楼层阅读，帖内引用 `>>123456` 跳转高亮
- 图片内联预览（点击放大，`Esc` 关闭）
- **帖子翻译**：免费（Google / MyMemory）+ AI（DeepSeek / 智谱 GLM / OpenAI / 通义千问）
- 自定义「高级设置」面板，集中配置各 AI 引擎 API Key（SecretStorage 安全存储）
- 收藏板块、SFW 过滤
